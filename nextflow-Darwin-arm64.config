params {
    runtime_opts = ""
}

profiles {

    standard {
        docker.enabled = true
        singularity.enabled = false
    }

}

docker {
    temp = "auto"
    runOptions = "--platform=linux/x86_64 -v $PWD:$PWD ${params.runtime_opts}"
}
